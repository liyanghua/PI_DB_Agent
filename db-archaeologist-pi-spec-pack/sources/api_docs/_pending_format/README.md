# 待处理格式文档

此目录存放因格式不兼容暂时无法入库的源文档。

## 智能体数仓完整接口文档_修复后逐接口完整格式版.md

**时间**：2026-06-19 回滚时从 `_inbox/` 移入

**原因**：新版文档格式与现有 extractor 不兼容，导致 rebuild 失败（159 APIs → 0 parsed）

**差异**：
- 缺少 `## 全量接口索引` 表（extractor 依赖此表生成 api_index_seed.json）
- 章节头格式变化：`## N. 名称` → `## N. METHOD 名称`（缺失 METHOD）
- 缺少 `### 基本信息` 表（path/module/source_line_no 无法提取）
- 参数表头变化：`名称|位置|类型|必选|说明` → `参数字段|数据类型|是否必填|说明|备注`（缺失位置列）
- 返回字段表头扩展：3 列 → 9 列（新增对象ID/类型、可打标签等语义信息）

**下一步**：
1. 定义格式契约规范（`docs/source_format_contract.md`）
2. 实现 `extract:lint` stage 前置校验
3. 实现 extractor v2 双轨支持（兼容旧格式 + 新格式）
4. 等新版文档按规范修复或 extractor v2 完成后再入库

**信息**：
- 大小：1078291 字节
- 接口数：159（与旧版数量一致）
- 包含 curl 示例、真实 query/body 参数、字段语义九列标注